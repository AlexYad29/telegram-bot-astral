# --- Telegram bot ---
aiogram==3.13.1

# --- Database ---
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# --- Cache / FSM storage ---
redis==5.2.0

# --- OpenAI ---
# >=1.55 — для совместимости с httpx 0.28 (`proxies` kwarg выпилен).
openai>=1.55,<2
# tiktoken — pre-flight подсчёт токенов промптов (ETAP 12, cost optimization).
tiktoken==0.8.0

# --- Scheduler ---
APScheduler==3.10.4

# --- Config / validation ---
pydantic==2.9.2
pydantic-settings==2.7.1

# --- Utils ---
python-dateutil==2.9.0.post0
tenacity==9.0.0
orjson==3.10.11
